@model IEnumerable<CommonWeal.Data.RegisteredUser>

<div class="row">
    <div class="col-md-12 ">
        <div class="requests">
            <table class="tableAdmin col-md-12">
                <thead class="tableheader col-md-12">
                    <tr>
                        <th class="col-md-4">User NAME</th>
                        <th class="col-md-4">Email Id</th>
                        <th class="col-md-4">Unblock</th>
                    </tr>
                </thead>
                @foreach (var item in Model)
                {                       
                    <tr class="admintable">
                        <td class="col-md-4 adminTableData">@Html.DisplayFor(modelItem => item.FirstName)</td>
                        <td class="col-md-4 adminTableData"><a>@Html.DisplayFor(modelItem => item.UserEmail) </a></td>
                        <td class="col-md-4 adminTableData"><a href="/admin/unblockUsers/@item.LoginID" class="fa fa-unlock fa-2x" aria-hidden="true" style="color:green"></a></td>
                    </tr>     
                }
                <i><a href="/Admin/" style="font-size: 16px;">Back to Dashboard</a></i>
            </table>
        </div>
    </div>
</div>
